---
import { Image } from "astro:assets";
import teacherSvg from "../assets/teacher.svg";
import booksSvg from "../assets/books.svg";
import dianaSvg from "../assets/diana.svg";
---

<section class="hero">
  <div class="stars-background">
    <div class="stars stars-small"></div>
    <div class="stars stars-medium"></div>
    <div class="stars stars-large"></div>
  </div>
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">
          <span class="brand">Alumtu</span>
          <span class="coming-soon">Próximamente</span>
        </h1>
        <p class="hero-subtitle">
          La plataforma que conectará estudiantes y profesores para clases
          privadas personalizadas
        </p>
        <p class="hero-description">
          Estamos trabajando en algo increíble. Una plataforma innovadora que
          revolucionará la forma en que estudiantes y profesores se conectan
          para el aprendizaje personalizado.
        </p>
        <div class="hero-actions">
          <a href="#newsletter" class="btn">¡Únete a la Lista de Espera!</a>
          <a href="/about" class="btn btn-outline">Conoce más</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="floating-card">
          <div class="card">
            <div class="card-icon">
              <Image
                src={teacherSvg}
                alt="Profesores"
                class="card-icon"
                loading="eager"
              />
            </div>
            <h3>Profesores</h3>
            <p>Expertos en su área</p>
          </div>
        </div>
        <div class="floating-card delay-1">
          <div class="card">
            <div class="card-icon">
              <Image
                src={booksSvg}
                alt="Estudiantes"
                class="card-icon"
                loading="eager"
              />
            </div>
            <h3>Estudiantes</h3>
            <p>Aprendizaje personalizado</p>
          </div>
        </div>
        <div class="floating-card delay-2">
          <div class="card">
            <div class="card-icon">
              <Image
                src={dianaSvg}
                alt="Clases"
                class="card-icon"
                loading="eager"
              />
            </div>
            <h3>Clases</h3>
            <p>Adaptadas a tus necesidades</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    padding-top: 80px;
    overflow: hidden;
  }

  .stars-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
  }

  .stars {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: transparent;
  }

  .stars-small {
    background-image: radial-gradient(2px 2px at 20px 30px, #fff, transparent),
      radial-gradient(
        2px 2px at 40px 70px,
        rgba(255, 255, 255, 0.8),
        transparent
      ),
      radial-gradient(1px 1px at 90px 40px, #fff, transparent),
      radial-gradient(
        1px 1px at 130px 80px,
        rgba(255, 255, 255, 0.6),
        transparent
      ),
      radial-gradient(2px 2px at 160px 30px, #fff, transparent);
    background-repeat: repeat;
    background-size: 200px 100px;
    animation: moveStars 20s linear infinite;
  }

  .stars-medium {
    background-image: radial-gradient(
        3px 3px at 100px 50px,
        rgba(255, 255, 255, 0.8),
        transparent
      ),
      radial-gradient(
        2px 2px at 150px 90px,
        rgba(255, 255, 255, 0.6),
        transparent
      ),
      radial-gradient(3px 3px at 40px 120px, #fff, transparent),
      radial-gradient(
        2px 2px at 180px 10px,
        rgba(255, 255, 255, 0.7),
        transparent
      );
    background-repeat: repeat;
    background-size: 250px 150px;
    animation: moveStars 30s linear infinite;
  }

  .stars-large {
    background-image: radial-gradient(
        4px 4px at 80px 20px,
        rgba(255, 255, 255, 0.9),
        transparent
      ),
      radial-gradient(
        3px 3px at 200px 100px,
        rgba(255, 255, 255, 0.7),
        transparent
      ),
      radial-gradient(4px 4px at 50px 150px, #fff, transparent);
    background-repeat: repeat;
    background-size: 300px 200px;
    animation: moveStars 40s linear infinite;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }

  .brand {
    color: white;
    display: block;
  }

  .coming-soon {
    background: linear-gradient(45deg, #ffffff, #e5e7eb);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: block;
    font-size: 2.5rem;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .hero-description {
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-visual {
    position: relative;
    height: 400px;
  }

  .floating-card {
    min-width: 200px;
    position: absolute;
    animation: float 6s ease-in-out infinite;
    z-index: 1;
  }

  .floating-card:nth-child(1) {
    top: 10%;
    left: 10%;
    z-index: 3;
  }

  .floating-card:nth-child(2) {
    top: 15%;
    right: 10%;
    z-index: 2;
  }

  .floating-card:nth-child(3) {
    bottom: 15%;
    left: 30%;
    z-index: 1;
  }

  .delay-1 {
    animation-delay: -2s;
  }

  .delay-2 {
    animation-delay: -4s;
  }

  .card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    padding: 2rem;
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    text-align: center;
    color: white;
    min-width: 180px;
  }

  .card-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    width: 58px;
    height: 58px;
    display: inline-block;
  }

  .card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
    font-weight: 600;
  }

  .card p {
    opacity: 0.9;
    font-size: 0.9rem;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes moveStars {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-200px);
    }
  }

  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
      text-align: center;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .coming-soon {
      font-size: 2rem;
    }

    .hero-visual {
      height: 450px;
    }

    .floating-card:nth-child(1) {
      top: 5%;
      left: 5%;
      z-index: 3;
    }

    .floating-card:nth-child(2) {
      top: 25%;
      right: 10%;
      z-index: 2;
    }

    .floating-card:nth-child(3) {
      bottom: 20%;
      left: 20%;
      z-index: 1;
    }

    .card {
      padding: 1.2rem;
      min-width: 120px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }

    .card-icon {
      width: 40px;
      height: 40px;
    }

    .card h3 {
      font-size: 1rem;
    }

    .card p {
      font-size: 0.8rem;
    }

    .hero-actions {
      justify-content: center;
    }
  }
</style>
