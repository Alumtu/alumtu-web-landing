---
// No imports needed as this is a standalone page
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <title>Página no encontrada - Alumtu</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <div class="error-card">
        <div class="error-icon">
          <svg class="error-svg" viewBox="0 0 100 100">
            <circle class="error-circle" cx="50" cy="50" r="45" fill="none"
            ></circle>
            <path class="error-path1" d="M30 30 L70 70" stroke-linecap="round"
            ></path>
            <path class="error-path2" d="M70 30 L30 70" stroke-linecap="round"
            ></path>
          </svg>
        </div>
        <h1 class="error-code">404</h1>
        <h2 class="title">¡Oops! Página no encontrada</h2>
        <p class="message">
          La página que estás buscando no existe o ha sido movida.
        </p>
        <p class="submessage">
          No te preocupes, puedes regresar a nuestra página principal.
        </p>
        <div class="buttons">
          <a href="/" class="btn-home">Ir al inicio</a>
          <button onclick="history.back()" class="btn-back">Regresar</button>
        </div>
      </div>
    </div>
  </body>
</html>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html,
  body {
    width: 100%;
    height: 100%;
    font-family: "Inter", sans-serif;
    line-height: 1.6;
  }

  body {
    background: linear-gradient(135deg, #0c2c63 25%, #b9b9ca 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }

  .container {
    width: 100%;
    max-width: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .error-card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 20px;
    padding: 3rem 2rem;
    text-align: center;
    box-shadow: 0 20px 40px rgba(12, 44, 99, 0.2);
    animation: slideIn 0.8s ease-out;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .error-icon {
    margin: 0 auto 1rem;
    width: 80px;
    height: 80px;
  }

  .error-svg {
    width: 100%;
    height: 100%;
  }

  .error-circle {
    stroke: #ef4444;
    stroke-width: 3;
    stroke-dasharray: 283;
    stroke-dashoffset: 283;
    animation: drawCircle 1s ease-in-out 0.2s forwards;
  }

  .error-path1,
  .error-path2 {
    stroke: #ef4444;
    stroke-width: 4;
    stroke-dasharray: 56;
    stroke-dashoffset: 56;
  }

  .error-path1 {
    animation: drawX1 0.5s ease-in-out 1.2s forwards;
  }

  .error-path2 {
    animation: drawX2 0.5s ease-in-out 1.4s forwards;
  }

  .error-code {
    font-size: 4rem;
    font-weight: 900;
    color: #0c2c63;
    margin-bottom: 1rem;
    animation: bounceIn 0.8s ease-out 0.4s both;
    text-shadow: 2px 2px 4px rgba(12, 44, 99, 0.1);
  }

  .title {
    font-size: 2rem;
    font-weight: 700;
    color: #0c2c63;
    margin-bottom: 1rem;
    animation: fadeInUp 0.6s ease-out 0.6s both;
  }

  .message {
    font-size: 1.2rem;
    color: #4b5563;
    margin-bottom: 0.5rem;
    animation: fadeInUp 0.6s ease-out 0.8s both;
  }

  .submessage {
    font-size: 1rem;
    color: #6b7280;
    margin-bottom: 2rem;
    animation: fadeInUp 0.6s ease-out 1s both;
  }

  .buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeInUp 0.6s ease-out 1.2s both;
  }

  .btn-home {
    display: inline-block;
    padding: 1rem 2rem;
    background: #0c2c63;
    color: white;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(12, 44, 99, 0.3);
  }

  .btn-home:hover {
    background: #1e40af;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(12, 44, 99, 0.4);
  }

  .btn-back {
    display: inline-block;
    padding: 1rem 2rem;
    background: transparent;
    color: #0c2c63;
    border: 2px solid #0c2c63;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: "Inter", sans-serif;
  }

  .btn-back:hover {
    background: #0c2c63;
    color: white;
    transform: translateY(-2px);
  }

  /* Animations */
  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateY(30px) scale(0.9);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes bounceIn {
    0% {
      opacity: 0;
      transform: scale(0.3);
    }
    50% {
      transform: scale(1.05);
    }
    70% {
      transform: scale(0.9);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes drawCircle {
    to {
      stroke-dashoffset: 0;
    }
  }

  @keyframes drawX1 {
    to {
      stroke-dashoffset: 0;
    }
  }

  @keyframes drawX2 {
    to {
      stroke-dashoffset: 0;
    }
  }

  /* Responsive design */
  @media (max-width: 640px) {
    .error-card {
      padding: 2rem 1.5rem;
      margin: 1rem;
    }

    .error-code {
      font-size: 3rem;
    }

    .title {
      font-size: 1.5rem;
    }

    .message {
      font-size: 1.1rem;
    }

    .error-icon {
      width: 60px;
      height: 60px;
    }

    .buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn-home,
    .btn-back {
      width: 100%;
      max-width: 200px;
    }
  }
</style>
